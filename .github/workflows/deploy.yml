name: Deploy TallerOS

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deploy
        run: |
          curl -s -X POST https://deploy.armracing.com/deploy
          echo "Deploy triggered"
